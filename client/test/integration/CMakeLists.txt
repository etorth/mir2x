# AI-generated integration tests for the client executable (e.g. a python script that
# launches client with some CLI args and checks its behavior/output) go here. register one
# with mir2x_add_integration_test(), e.g.:
#
#   mir2x_add_integration_test(
#       NAME client_smoke
#       COMMAND python3 ${CMAKE_CURRENT_SOURCE_DIR}/test_client.py
#       DEPENDS client)
#
# none exist yet, so nothing is registered here for now.
