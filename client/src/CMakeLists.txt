AUX_SOURCE_DIRECTORY(. CLIENT_SRC)
ADD_EXECUTABLE(client ${CLIENT_SRC})

install(TARGETS client DESTINATION ${BIN_DIR})

TARGET_INCLUDE_DIRECTORIES(client PRIVATE ${COMMON_SOURCE_DIR})

TARGET_LINK_LIBRARIES(client g3logger        )
TARGET_LINK_LIBRARIES(client tinyxml2        )
TARGET_LINK_LIBRARIES(client pthread         )
TARGET_LINK_LIBRARIES(client zip             )
TARGET_LINK_LIBRARIES(client ${CMAKE_DL_LIBS})
TARGET_LINK_LIBRARIES(client common          )
TARGET_LINK_LIBRARIES(client SDL2            )
TARGET_LINK_LIBRARIES(client SDL2_ttf        )
TARGET_LINK_LIBRARIES(client SDL2_image      )
TARGET_LINK_LIBRARIES(client ${LUA_LIBRARIES})
TARGET_LINK_LIBRARIES(client iconv      )
