AUX_SOURCE_DIRECTORY(. CLIENT_SRC)
ADD_EXECUTABLE(client ${CLIENT_SRC})

TARGET_INCLUDE_DIRECTORIES(client PRIVATE ${COMMON_SOURCE_DIR})

TARGET_LINK_LIBRARIES(client dl    )
TARGET_LINK_LIBRARIES(client common)
